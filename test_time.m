% test time
%for i = 1:5
    %disp(i);
    i=0;
   % iniTime = clock;
    %limit = 1;
    t=tic;
    while(toc(t)<1)
        
    end
%     while etime(clock,iniTime) < limit
%              %disp(clock);
%              i=i+1;
% %         disp(iniTime);
%     end
    toc(t)
    %dist(X_r,Y_r,Z_r);
    disp(1);
%end